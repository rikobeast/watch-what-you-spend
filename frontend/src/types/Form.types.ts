export type FormInfoType = {
  productPrice: number;
  productName: string;
};

export type FromInfoTypeTwo = {
  productPrice: {
    value: string;
    inputName: string;
  };
  productName: {
    value: string;
    inputName: string;
  };
};
